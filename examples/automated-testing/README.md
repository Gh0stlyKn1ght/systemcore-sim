# Automated regression suite

Run:

```bash
npm run test:automated
```

The suite checks freshness boundaries, invalid data, disable behavior, deadband behavior, output invariants across 1,681 input pairs, and a controlled mutation of the 100 ms rule.

The same contracts should be translated into JUnit 5 inside the selected WPILib 2027 project.
